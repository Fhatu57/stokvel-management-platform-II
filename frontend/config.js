// Public browser configuration.
// Supabase's anon key is designed for browser use; database access must still
// be protected with Row Level Security policies. Never put a service-role key
// in this file.
window.STOKVEL_CONFIG = Object.freeze({
  supabaseUrl: '',
  supabaseAnonKey: '',
});
